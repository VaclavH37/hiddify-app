import 'package:flutter/material.dart';

/// Rayn VPN design tokens — shared shadow stacks.
class RaynShadows {
  const RaynShadows._();

  static const List<BoxShadow> ambient = [BoxShadow(color: Color(0x66000000), blurRadius: 24, offset: Offset(0, 8))];
}
